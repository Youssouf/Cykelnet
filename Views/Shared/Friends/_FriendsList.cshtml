@model List<Cykelnet.VersionedUser>

@if (Model.Count > 0)
{
    <h4>Friends</h4>
    <ul class="friend-list">
    @foreach (var item in Model)
    {
        <li>
            @{Html.RenderPartial("Friends/_Friend", item);}
        </li>
    }
    </ul>
}